# SigExt Longformer – Final

This branch contains the final SigExt Longformer training and inference code.

## Files
- `train_sigext_longformer_new.py` : main training + inference script
- `requirements.txt` : dependencies
- `SigExt/` : original SigExt code

## How to run
```bash
pip install -r requirements.txt
python train_sigext_longformer_new.py
